#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../codemod-imports-sort/bin/codemod-imports-sort" "$@"
  ret=$?
else 
  node  "$basedir/../codemod-imports-sort/bin/codemod-imports-sort" "$@"
  ret=$?
fi
exit $ret
