"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.prepareJscodeshift = prepareJscodeshift;
var jscodeshift = require("jscodeshift");
var getParser = require("jscodeshift/dist/getParser");

var initOptions = {
  parser: "flow"
};

function prepareJscodeshift() {
  var options = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : initOptions;

  // if (parser) {
  //   return jscodeshift.withParser(parser);
  // } else
  if (options.parser) {
    return jscodeshift.withParser(getParser(options.parser));
  } else {
    return jscodeshift;
  }
}